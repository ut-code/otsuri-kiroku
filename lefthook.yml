pre-commit:
  parallel: true
  jobs:
    - name: type-check
      run: bun check:type
      stage_fixed: true

    - name: lint
      run: bun check:lint
      glob: "*.{js,ts,jsx,tsx,svelte}"
      stage_fixed: true

    - name: format-check
      run: bun check:format
      stage_fixed: true
